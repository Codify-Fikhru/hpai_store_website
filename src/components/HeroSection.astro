<section class="hero">
  <div class="hero-content">
    <h1 class="hero-title">HPAI Codify Store</h1>
    <p class="hero-subtitle">Temukan Produk Halal Berkualitas</p>
    <button class="cta-button">Belanja Sekarang</button>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    width: 100%;
    margin-top: -64px;
    padding-top: 64px;
    background-image: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.7)),
      url("https://i.ytimg.com/vi/7v3BMkvl4E4/maxresdefault.jpg");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: white;
  }

  .hero-content {
    padding: 2rem;
    max-width: 800px;
    margin: 0 auto;
    animation: fadeIn 1s ease-out;
  }

  .hero-title {
    font-size: 4rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
    font-family: "Poppins", sans-serif;
  }

  .hero-subtitle {
    font-size: 1.8rem;
    margin-bottom: 2.5rem;
    font-weight: 300;
    letter-spacing: 1px;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2);
    font-family: "Poppins", sans-serif;
  }

  .cta-button {
    background-color: #14b8a6;
    color: white;
    padding: 1.2rem 2.5rem;
    border: none;
    border-radius: 25px;
    font-size: 1.2rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 1px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }

  .cta-button:hover {
    background-color: #14b8a6;
    transform: translateY(-2px);
    box-shadow: 0 6px 8px rgba(0, 0, 0, 0.2);
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.4rem;
    }

    .cta-button {
      padding: 1rem 2rem;
      font-size: 1.1rem;
    }
  }
</style>
